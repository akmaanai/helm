apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-hook.fullname" . }}
  labels:
    {{- include "demo-hook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-hook.selectorLabels" . | nindent 4 }}
